config {
    type: "assertion"
}

SELECT
  f.*
FROM
  ${ref(dataform.projectConfig.defaultSchema, "fct_order_item")} f
LEFT JOIN
  ${ref(dataform.projectConfig.defaultSchema, "dim_product")} dp
ON
  dp.product_id = f.product_id
WHERE
  dp.product_id IS null
